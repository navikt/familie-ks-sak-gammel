apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: familie-ks-sak
  namespace: default
  labels:
    team: teamfamilie

spec:
  image: repo.adeo.no:5443/familie-ks-sak:RELEASE_VERSION
  port: 8083
  liveness:
    path: /internal/status/isAlive
    initialDelay: 30
  readiness:
    path: /internal/status/isAlive
    initialDelay: 30
  prometheus:
    enabled: true
    path: /internal/prometheus
  vault:
    enabled: true
  replicas:
    min: 2
    max: 4
    cpuThresholdPercentage: 50
  resources:
    limits:
      memory: 512Mi
      cpu: 500m
    requests:
      memory: 256Mi
      cpu: 200m
  secureLogs:
    enabled: true
  ingresses: # Optional. List of ingress URLs that will route HTTP traffic to the application.
    - https://familie-ks-sak.nais.preprod.local
